//////////////////////////////////////////////////////
//  Code Wars: FILTERED LIST
//////////////////////////////////////////////////////

function filter_list(l) {
    for (let x=0; x<l.length; x++) {
        console.log (typeof(l[x]);)
    }
}

filter_list([1,2,'a','b']);